## Build Workflow
When modifying the application codebase, you must always run the appropriate build commands yourself (e.g., `npm run electron:build`) to compile the final executable or production build. 
Do not leave the final compilation step to the user or instruct them to run the build command manually. Ensure that the final artifacts are successfully generated before concluding your task.
